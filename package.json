{
  "name": "taskmg-worker-site",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for running the static site on Cloudflare Worker.",
  "main": "workers-site/index.js",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0",
    "firebase": "^10.10.0",
    "chart.js": "^4.4.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite": "^5.2.11",
    "tailwindcss": "^3.4.3",
    "postcss": "^8.4.38",
    "autoprefixer": "^10.4.19"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm-run-all --parallel start:*",
    "deploy": "npm run build && wrangler deploy"
  }
}