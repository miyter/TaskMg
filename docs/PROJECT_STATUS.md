# TaskMg プロジェクト状況ドキュメント

**最終更新**: 2025-12-30

---

## 🏗️ 技術スタック

| カテゴリ | 技術 | バージョン |
|----------|------|------------|
| **フレームワーク** | React | 18.3.1 |
| **ビルドツール** | Vite | 6.0.0 |
| **言語** | TypeScript | 5.9.3 |
| **状態管理** | Zustand | 5.0.9 |
| **スタイリング** | Tailwind CSS | 3.4.3 |
| **バックエンド** | Firebase Firestore | 10.14.1 |
| **D&D** | @dnd-kit | core 6.3.1, sortable 10.0.0 |

---

## 📁 ディレクトリ構造

```
src/
├── App.tsx              # React アプリケーションルート (DndContext 管理)
├── main.tsx             # エントリーポイント
│
├── components/          # ★ React コンポーネント (推奨)
│   ├── common/          # Modal.tsx, SortableItem.tsx
│   ├── modals/          # ModalManager, TaskDetailModal, etc.
│   ├── sidebar/         # Sidebar, ProjectList, BasicFilters
│   └── tasks/           # TaskList, TaskItem
│
├── ui/                  # ⚠️ レガシー層 (移行中)
│   ├── core/            # AppInitializer, EventManager
│   ├── features/        # sidebar (移行済み/停止中), wiki, target-dashboard
│   └── layout/          # Vanilla TS による土台レイアウト
│
├── store/               # Zustand & Firestore 連携
│   ├── ui/              # modal-store, ui-store, filter-store
│   └── schema.ts        # Zod スキーマ
│
└── hooks/               # useTasks, useProjects, useLabels, useTimeBlocks
```

---

## 🔄 移行状況

### モーダルシステム (完了 ✅)
- **状況**: `task-modal.ts`, `modal-dom-generator.ts` を含むすべての旧バニラJSモーダルを削除。
- **実装**: `ModalManager.tsx` + `modal-store.ts` による React 実装へ完全移行。
- **互換性**: 非Reactコードからは `openModalDirect` を通じて呼び出し可能。

### サイドバー D&D (完了 ✅)
- **状況**: `sidebar-drag-drop.ts` を削除。`sidebar-view.ts` をスタブ化。
- **実装**: `@dnd-kit` を使用し、`App.tsx` レベルでドラッグエンドを制御。
- **機能**: タスクのプロジェクト移動、インボックス移動、プロジェクトの並び替えに対応。

### TypeScript 化 (完了 ✅)
- **状況**: 全 JS ファイルの TS 移行を完了。
- **ビルド**: `npx tsc --noEmit` パスを確認済み。

---

## 🎯 次のステップ

1. **サイドバー描画の React 化**
   - `sidebar-renderer.ts` (innerHTML方式) を `Sidebar.tsx` 内での React 描画に置き換える。
2. **レガシーレイアウトの廃止**
   - `layout.ts` による `innerHTML` 構築をやめ、`index.html` または `App.tsx` で全構造を定義する。
3. **スタブファイルの完全削除**
   - `sidebar-view.ts` などのスタブ化したレガシーコードを完全に除去する。

---

## 🔧 開発コマンド

```bash
# 開発サーバー
npm run dev

# 型チェック
npx tsc --noEmit

# ビルド
npm run build
```
