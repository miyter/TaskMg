/**
 * レイアウトに関する定数の一元管理
 */
export const LAYOUT_CONFIG = {
    // SIDEBAR_WIDTH は sidebar-constants.js に集約したため削除
    Z_INDEX: {
        HEADER: 10,
        OVERLAY: 20,
        SIDEBAR: 30,
        DROPDOWN: 40,
        MODAL: 50
    }
    // 未使用の SELECTORS は削除
};