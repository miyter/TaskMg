/**
 * System-wide constants (non-UI)
 * UI-specific constants should go to ui-constants.ts
 */

export const SYSTEM_CONSTANTS = {
    TIMEOUT: {
        AUTH_LOADING: 10000,
    },
    TIME_BLOCK: {
        MAX_COUNT: 10,
    }
} as const;
